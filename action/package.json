{
  "name": "@agentlint/action",
  "version": "0.0.0",
  "private": true,
  "description": "Reusable GitHub Action that runs the agentlint gate on pull requests.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "22.20.4",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
