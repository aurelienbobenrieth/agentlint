---
"@aurelienbbn/agentlint": patch
---

Resolve explicit files against the canonical repository path so macOS temporary directories and repository directory aliases are not mistaken for paths outside the repository.
