---
"@aurelienbbn/agentlint": patch
---

Ship the changelog and third-party notices in the package, test the exact minimum Node version in CI, and publish from the protected `npm` GitHub environment through npm trusted publishing instead of a long-lived token.
