---
"@aurelienbbn/agentlint": minor
---

Evaluate change rules against the merge base of an explicit or detected Git base and the complete working tree, including staged, unstaged, and untracked files. Detectors receive a normalized `ChangeSet` with file status, before/after snapshots, and hunks, and report a stable `key`, material `evidence`, and optional `lineageKey`.
