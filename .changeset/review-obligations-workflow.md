---
"@aurelienbbn/agentlint": minor
---

Add explicit plugin preset initialization, a versioned next-obligation JSON handoff, related-file review groups, independent review before revealing prior justifications, and measurable calibration reports with aggregation. Fix calibration review scoping. Review artifacts now use version 3 and browser draft storage version 4; regenerate earlier artifacts. Acceptance and fingerprint formats remain unchanged. Add the browser-safe calibration subpath and update packaged skills.
