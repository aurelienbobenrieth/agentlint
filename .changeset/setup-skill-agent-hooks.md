---
"@aurelienbbn/agentlint": minor
---

Add a `setup` skill that installs agentlint in a repository: verified repository-owned rule-package presets, gradual adoption without a warning level, and a copyable Claude Code and Codex Stop hook script (`agentlint-gate.mjs`) over the CLI exit code.
